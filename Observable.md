# Observables are part of RxJS and are used for handling asynchronous operations (e.g., HTTP requests, event handling).

- Ex -
- this.http.get('https://api.example.com/users').subscribe(data=> console.log(data));
